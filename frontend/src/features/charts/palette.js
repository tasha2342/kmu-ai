/**
 * 차트 색상 슬롯입니다. (검증된 값 — 임의로 바꾸지 마세요)
 *
 * - 범주형 2계열(질문 수 ↔ 미응답 수): 인접쌍 색각이상 ΔE 24.7 / 일반시야 ΔE 33.6 통과
 * - 순서형 5단계(만족도 1~5점): 단일 색상·명도 단조 증가·인접 명도차 ≥ 0.06 통과
 *
 * 규칙
 * - 계열 수가 늘어도 색을 순환시키지 않습니다. (범주가 많으면 단일 색상 + 축 라벨로 구분)
 * - 수치·라벨 텍스트에는 계열 색을 쓰지 않습니다. (본문 잉크 색 유지)
 */

/** 범주형 슬롯 (고정 순서) */
export const SERIES = ['#2a78d6', '#eb6834', '#1baf7a']

/** 크기(magnitude)를 나타내는 단일 색상 */
export const MAGNITUDE = '#2a78d6'

/** 순서가 있는 5단계 (만족도 1점 → 5점) */
export const ORDINAL_5 = ['#86b6ef', '#5598e7', '#2a78d6', '#1c5cab', '#104281']

/** 눈금·격자 등 물러나 있어야 하는 요소 */
export const GRID = '#e5e7eb'
export const AXIS_TEXT = '#6b7280'
export const SURFACE = '#ffffff'
